<Window x:Class="TextExpander.App.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TextExpander" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 상단 툴바 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <Button Content="엔진 토글" Command="{Binding ToggleEngineCommand}" Width="100" Margin="0,0,10,0"/>
                <Button Content="✔ 적용" Command="{Binding ApplyChangesCommand}" Width="80" Margin="0,0,5,0" 
                        Background="LightGreen" FontWeight="Bold"/>
                <Button Content="✖ 취소" Command="{Binding CancelChangesCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="⚙ 설정" Command="{Binding OpenSettingsCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="📤 내보내기" Command="{Binding ExportSnippetsCommand}" Width="90" Margin="0,0,5,0"/>
                <Button Content="📥 가져오기" Command="{Binding ImportSnippetsCommand}" Width="90" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"
                           Foreground="DarkBlue"/>
            </StackPanel>
        </StackPanel>
        
        <!-- 메인 컨텐츠 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 카테고리 리스트 -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="카테고리" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                
                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding Categories}" 
                         SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                         SelectionChanged="CategoryListBox_SelectionChanged"
                         MouseDoubleClick="CategoryListBox_DoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{Binding IsEnabled}" 
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center"/>
                                <Border Background="{Binding BackgroundBrush}" 
                                        CornerRadius="4" 
                                        Padding="8,4"
                                        BorderBrush="LightGray"
                                        BorderThickness="1">
                                    <TextBlock Text="{Binding Name}" 
                                               Foreground="{Binding ForegroundBrush}"
                                               FontWeight="Medium"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="추가" Command="{Binding AddCategoryCommand}" Width="50" Margin="0,0,5,0"/>
                    <Button Content="수정" Command="{Binding EditCategoryCommand}" Width="50" Margin="0,0,5,0"/>
                    <Button Content="삭제" Command="{Binding DeleteCategoryCommand}" Width="50"/>
                </StackPanel>
            </Grid>
            
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="LightGray"/>
            
            <!-- 스니펫 리스트 -->
            <Grid Grid.Column="2" Margin="5,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="스니펫" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Width="200" Margin="10,0,0,0" 
                             VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <SolidColorBrush Color="LightGray" Opacity="0.3"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                    <TextBlock Text="🔍" Margin="5,0,0,0" VerticalAlignment="Center" FontSize="12" Opacity="0.5"/>
                </StackPanel>
                
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding FilteredSnippets}"
                          SelectedItem="{Binding SelectedSnippet, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="All"
                          HeadersVisibility="Column">
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="{Binding CategoryBackgroundBrush}"/>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="활성" Binding="{Binding IsEnabled}" Width="50"/>
                        <DataGridTextColumn Header="키워드" Binding="{Binding Keyword}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding CategoryForegroundBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="대체 텍스트" Binding="{Binding Replacement}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding CategoryForegroundBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="메모" Binding="{Binding Note}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding CategoryForegroundBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
                
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="추가" Command="{Binding AddSnippetCommand}" Width="70" Margin="0,0,5,0"/>
                    <Button Content="삭제" Command="{Binding DeleteSnippetCommand}" Width="70"/>
                </StackPanel>
            </Grid>
        </Grid>
        
        <!-- 하단 상태 바 -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="TextExpander - 전역 텍스트 확장기"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>

