# TextExpander - 개발 & 빌드 가이드

## 🚀 개발 모드 실행 (권장)

### 명령어
```bash
cd C:\ai\projects\text-expender\src
dotnet run --project TextExpander.App
```

### 특징
- ✅ 코드 변경 즉시 반영
- ✅ 디버깅 메시지 확인 가능
- ✅ 빌드 시간 짧음
- ✅ 빠른 개발 사이클

---

## 📦 Release 빌드 (배포용)

### 방법 1: 타임스탬프 폴더로 빌드 (권장)
```powershell
cd C:\ai\projects\text-expender\src
$timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
$outputPath = "C:\ai\projects\text-expender\publish_$timestamp"
dotnet publish TextExpander.App -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o $outputPath
Write-Host "`n✅ 빌드 완료: $outputPath\TextExpander.exe" -ForegroundColor Green
Start-Process "explorer.exe" -ArgumentList $outputPath
```

### 방법 2: 기존 폴더 완전 삭제 후 빌드
```powershell
cd C:\ai\projects\text-expender
Remove-Item -Recurse -Force publish -ErrorAction SilentlyContinue
dotnet publish src\TextExpander.App -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o publish
Write-Host "`n✅ 빌드 완료: publish\TextExpander.exe" -ForegroundColor Green
```

### 실행
```bash
# 타임스탬프 폴더의 경우 (빌드.bat 사용 시)
C:\ai\projects\text-expender\publish\20250127_153045\TextExpander.exe

# 또는 빌드.bat 실행 후 자동으로 탐색기 열림
```

---

## 🧪 테스트 실행

```bash
cd C:\ai\projects\text-expender\src
dotnet test
```

---

## 📝 사용법

### 1. 앱 실행
- 개발 모드: `dotnet run --project TextExpander.App`
- 또는: `publish\TextExpander.exe`

### 2. 스니펫 추가
1. 카테고리 추가/선택
2. 스니펫 추가 버튼 클릭
3. DataGrid에서 더블클릭하여 수정:
   - **키워드**: `;home` (세미콜론으로 시작 권장)
   - **대체 텍스트**: `서울특별시 강남구 테헤란로 123`
   - **활성**: ✓ 체크
4. **✔ 적용** 버튼 클릭 ← **필수!**
5. "적용 완료! ✓" 메시지 확인

### 3. 설정 (⚙ 버튼)
1. **구분자 키 설정**
   - 스페이스, 엔터, 탭, 마침표, 쉼표, 느낌표, 물음표, 콜론 중 선택
   - 체크 해제한 키는 스니펫 트리거로 작동하지 않음
   
2. **효과음**
   - 스니펫 확장 시 알림음 재생 활성화/비활성화
   
3. **성능 (저사양 PC용)**
   - 키 입력 처리 간격 설정 (기본: 0ms = 즉시 처리)
   - 저사양: 10-20ms 권장 (CPU 부하 감소)

4. **시작 프로그램**
   - Windows 시작 시 자동 실행 옵션
   - 체크 시 Windows 로그인 시 TextExpander 자동 실행

### 4. 검색 기능
- 스니펫 목록 상단의 검색창에서 키워드/대체 텍스트/메모 검색
- 실시간 필터링 (입력 즉시 결과 표시)

### 5. 가져오기/내보내기
- **📤 내보내기**: 현재 스니펫 데이터를 JSON 파일로 백업
- **📥 가져오기**: JSON 파일에서 스니펫 데이터 복원
  - 예: 기존 데이터 삭제 후 가져오기
  - 아니오: 기존 데이터에 병합

### 4. 테스트
- 메모장 열기
- `;home ` 입력 (스페이스 포함)
- "서울특별시 강남구 테헤란로 123"로 자동 변환 확인

---

## ⚠️ 주의사항

1. **적용 버튼 필수**: DataGrid에서 수정만 하면 저장 안됨!
2. **카테고리 활성화**: 카테고리 체크박스 ✓ 확인
3. **스니펫 활성화**: 스니펫 "활성" 컬럼 ✓ 확인
4. **엔진 상태**: 상단에 "동작 중" 확인 (일시 정지 아님)
5. **구분자 설정**: 설정 창에서 원하는 구분자 키 활성화 확인
6. **구분자 입력**: 키워드 뒤에 활성화된 구분자 키 입력 필요

---

## 🐛 문제 해결

### 빌드 파일이 업데이트 안 될 때
```bash
# 1. 실행 중인 프로세스 종료
taskkill /F /IM TextExpander.exe

# 2. bin/obj 폴더 삭제
cd C:\ai\projects\text-expender\src
Remove-Item -Recurse -Force TextExpander.App\bin, TextExpander.App\obj

# 3. publish 폴더 삭제
Remove-Item -Recurse -Force C:\ai\projects\text-expender\publish

# 4. 다시 빌드
dotnet publish TextExpander.App -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o C:\ai\projects\text-expender\publish
```

### 스니펫이 작동하지 않을 때
1. 적용 버튼을 눌렀는지 확인
2. 카테고리와 스니펫 모두 활성화되어 있는지 확인
3. 상단 상태가 "동작 중"인지 확인
4. 설정(⚙)에서 원하는 구분자 키가 활성화되어 있는지 확인
5. 키워드 뒤에 활성화된 구분자(스페이스 등)를 입력했는지 확인
6. 일부 앱에서는 관리자 권한 필요할 수 있음

---

## 📊 프로젝트 구조

```
TextExpander/
├── src/
│   ├── TextExpander.Core/           # 도메인 로직
│   │   ├── Domain/                  # Category, Snippet
│   │   ├── Engine/                  # ExpanderEngine
│   │   ├── Models/                  # DTO, AppSettings
│   │   └── Repositories/            # JsonSnippetRepository, JsonSettingsRepository
│   ├── TextExpander.Core.Tests/     # 단위 테스트
│   └── TextExpander.App/            # WPF UI
│       ├── Services/                # Win32 후킹, 키보드 출력, 효과음
│       ├── ViewModels/              # MVVM
│       └── Windows/                 # MainWindow, SettingsWindow
└── publish/
    └── TextExpander.exe             # 배포 파일
```

---

## 🎯 핵심 파일

- **스니펫 데이터**: `%AppData%\TextExpander\snippets.json`
- **설정 데이터**: `%AppData%\TextExpander\settings.json`
- **메인 진입점**: `src/TextExpander.App/App.xaml.cs`
- **확장 엔진**: `src/TextExpander.Core/Engine/ExpanderEngine.cs`
- **키보드 후킹**: `src/TextExpander.App/Services/GlobalKeyboardHook.cs`
- **키보드 출력**: `src/TextExpander.App/Services/KeyboardOutputService.cs`
- **효과음 서비스**: `src/TextExpander.App/Services/SoundService.cs`

---

**개발할 때는 항상 `dotnet run --project TextExpander.App` 사용하세요!**

